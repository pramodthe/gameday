# Core API
fastapi>=0.115
uvicorn>=0.30
pydantic>=2.7
httpx>=0.27
python-dotenv>=1.0

# Tests
pytest>=8.0

# Voice + AI (used as later tasks land: STT/TTS, extraction, coaching)
deepgram-sdk>=3.5
anthropic>=0.40
websockets>=12.0
